# ─── Firebase ───────────────────────────────────────────────
# Path to your serviceAccountKey.json (download from Firebase Console)
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# ─── Server ─────────────────────────────────────────────────
PORT=3001
CLIENT_URL=http://localhost:5173

# ─── PostgreSQL (only needed for migration — can delete after) ──
DB_HOST=127.0.0.1
DB_PORT=5434
DB_NAME=office_manager
DB_USER=postgres
DB_PASSWORD=marshal
